{% extends "base.twig" %}

{% block content %}
	<div class="about__content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<div class="container">
					<h1 class="about__heading">{{post.title}}</h1>
					<div class="article-body">
						{{post.content}}
					</div>
				</div>
			</section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}